# Copy to .env.test (gitignored) and fill in. These run against a real Supabase
# project — never point at staging or production. The RLS tests INSERT and DELETE
# rows under test-prefixed slugs and email addresses.

SUPABASE_TEST_URL=
SUPABASE_TEST_ANON_KEY=
SUPABASE_TEST_SERVICE_KEY=
